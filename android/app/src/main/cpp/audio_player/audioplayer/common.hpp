// Simplified version to avoid build errors
// Empty implementations that do nothing

#define CPP20_AVAILABLE 0
#define DEBUG false

// Empty debug print functions that do nothing
#define debugPrint(...) do {} while(0)
#define printDebug(...) do {} while(0)